<% 
  const pageTitle = typeof pageTitle !== 'undefined' ? pageTitle : 'Suministros Herco';
  const bodyClass = typeof bodyClass !== 'undefined' ? bodyClass : '';
  const robots = typeof robots !== 'undefined' ? robots : 'all';
%>